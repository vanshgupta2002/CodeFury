// JavaScript for dynamic behavior (if needed)
document.addEventListener('DOMContentLoaded', () => {
    // Example: Highlight a meeting happening soon
    const meetings = document.querySelectorAll('.meeting-item');
    meetings.forEach((meeting) => {
      // Add logic to dynamically style meetings based on time
    });
  });
  